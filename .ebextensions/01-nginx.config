files:
  /etc/nginx/conf.d/security.conf:
    mode: "000644"
    owner: root
    group: root
    content: |
      # http directive
      server_tokens off;

      # For more settings?
      # should adding here.
