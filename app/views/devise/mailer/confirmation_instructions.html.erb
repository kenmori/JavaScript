<p><%= t '.greeting', recipient: @email %></p>

<p><%= t '.instruction' %></p>

<p><%= link_to t('.action'), confirmation_url(@resource, confirmation_token: @token, input_password: @user.no_password_required) %></p>
