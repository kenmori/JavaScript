import OKRCreationFlow from '../components/OKRCreationFlow';
import { connect } from 'react-redux';

const mapStateToProps = (_state) => {
  return {};
};

const mapDispatchToProps = _dispatch => {
  return {};
};

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(OKRCreationFlow);
