# JavaScript練習問題集 改修計画 (plan.md)

JavaScript練習問題集(`JavaScript.md`)の2026年アップデートに関する作業計画と進捗管理のためのドキュメントです。

## ゴール

- ECMAScript2026までの新機能をカバーした問題集にする
- 動かないコード例・誤記をなくし、学習者がそのままコンソールで試せる状態にする
- 問題番号や表記の揺れを整理し、メンテナンスしやすい構成にする

## 完了 (2026/8/27)

※「問題の追加」「修正」の項目中の問題番号は作業当時(リナンバリング前)の旧番号。ES2026の問401〜404は現在の問408〜411、ES2025の問398〜400は現在の問405〜407にあたる

### 問題の追加

- [x] ES2026セクション「問401〜(ECMAScript2026)」を新設
  - [x] 問401: `Error.isError` — realmをまたいでも確実なError判定
  - [x] 問402: `using`宣言 — `[Symbol.dispose]()`による自動リソース解放
  - [x] 問403: `await using` — `[Symbol.asyncDispose]()`による非同期リソース解放
  - [x] 問404: `DisposableStack` — 複数の後始末処理を逆順で実行
- [x] 未収録だったES2025の問題を追加
  - [x] 問398: Iterator Helpers (`map`/`take`/`toArray`)
  - [x] 問399: `Promise.try`
  - [x] 問400: `RegExp.escape`

### 修正

- [x] 解答の欠落を補完(問397: 配列のローテーション、問81: 多次元配列+'san')
- [x] 動かないコード例の修正
  - 問11: sortの比較関数が真偽値を返していた → `a.id - b.id`
  - 問28: `Objcet.isExtensible` → `Object.isExtensible`(返り値`false`に合わせて問題文も修正)、`Object.key` → `Object.keys`
  - 問51: `new Map(fafa)`の定義漏れ
  - 問58: `Boolean(Math)`の閉じ括弧不足
  - 問73: `import * as module`の名前欠落
- [x] 誤った出力例の修正(問15のsplice後の配列、問391の`month`→`months`)
- [x] タイポ修正(`sroces`→`sources`、`undefinde`、`globale`、`カーリー化`→`カリー化`、`letarry`、`.fseventsd/`の混入除去 など)
- [x] メタ情報の更新(更新情報、前提の対応ECMAScriptバージョンに2026を追記、問396の「ES2025 state 4」→「ES2025」)

### 構成の整理

- [x] 問題番号の重複を解消するリナンバリング
  - 重複は計8組あった(旧問21、問31、問90、問260、問284、問323、問386、問387)。ファイル順に全411問の連番(問1〜問411)に振り直した
  - 本文中の相互参照7箇所も新番号に更新(問49→問51、問50→問52、問83→問84、問157→問159、問342→問347、問350→問355、問352→問357)
- [x] 「問」と「問題」の表記揺れを統一(すべて「問」表記に統一)
- [x] 空だった旧問64を削除(リナンバリングで欠番なし)
- [x] `<details>`ブロックを50問単位で再分割(問1〜問50 … 問351〜問400、問401〜問407、問408〜(ECMAScript2026)の10ブロック)

## TODO (次回以降)

### 内容の拡充

- [ ] ES2025の残りの機能の問題を追加
  - [ ] 重複名前付きキャプチャグループ(`(?<year>...)`の再利用)
  - [ ] 正規表現パターン修飾子(インラインフラグ `(?i:...)`)
  - [ ] `Float16Array` / `Math.f16round`
  - [ ] JSONモジュール / Import Attributes (`import ... with { type: "json" }`)
  - [ ] Iterator Helpersの続編(`filter`/`drop`/`flatMap`/`reduce`)
- [ ] ES2026のStage 4入りを確認して問題を追加(TC39のfinished proposalsを確認)
- [ ] `Map.groupBy`の問題を追加(問394の`Object.groupBy`と対にする)

### 品質改善

- [ ] 全問題のコード例をNode.js最新版で実行して動作確認する
- [ ] 古い外部リンク(JS Bin、jsbin.comの答えリンク等)の生死確認と差し替え
- [ ] 問38(strict mode)など長文解答の整形・タイポ修正
- [ ] 参照記事リストの整理(リンク切れの除去)

## 運用メモ

- 作業ブランチ: `claude/2026-problems-dszfq8`
- 問題を追加したら冒頭の「更新情報」に日付付きで追記する
- 新しいES機能の問題には `// ES20XX` のコメントとMDNへのリンクを付ける
- 元の配列・オブジェクトを破壊しない系のメソッドは「元を破壊しないこと」を問題文の制約として明記するスタイルを踏襲する
